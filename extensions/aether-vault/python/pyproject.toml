[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "aether-vault"
version = "1.0.0"
description = "Erasure-coded encrypted distributed backup for the Aether protocol"
requires-python = ">=3.11"
license = { text = "MIT" }
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21",
]

[tool.hatch.build.targets.wheel]
packages = ["aether_vault"]
